//the sign up screen users that have no token and elect to sign up are brought here, facebook
// button, also a 'why were requiring facebook auth.' section'
import React, { Componenet, View, Text, StyleSheet}

export default class SignUp extends Component {
	render () {
		return (

		)
	}
};

const styles = StyleSheet.create({
	container: {
		flex:1
	}
})